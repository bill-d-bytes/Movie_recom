[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    ignore::UserWarning
    ignore::urllib3.exceptions.NotOpenSSLWarning
markers =
    unit: Fast tests with no or minimal I/O
    integration: Flask client + database (and ML when present)
addopts = -ra
